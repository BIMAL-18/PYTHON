# can you changes the value inside a list which is contained in set s 
# s = ( 8 , 7 , 12, "Bimal",[1,2])
s = ( 8 , 7 , 12, "Bimal",[1,2])

print(s)
# we cannot change the value of the list inside the set
# because set in python require all their elements to be immutable and hashable
